generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Tool {
  id          Int      @id @default(autoincrement())
  name        String
  description String
  isAvailable Boolean  @default(true)
}
